import { getURL } from "./essentialsScraping";
import {url} from "../types/url"

export function analyzeLink() : string{

    try {
        const URL = getURL();
        
        
    } catch (error) {
        
    }
}